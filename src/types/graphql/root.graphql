type Query {
  posts: [Post]
  secrets: [Secret]
}

type Acknowledgment {
  success: Boolean
}

type Mutation {
  dispatch(
    action: ActionInput!
  ): Acknowledgment
}

schema {
  query: Query
  mutation: Mutation
}
